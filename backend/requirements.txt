fastapi==0.104.1   ##web framework used to build the API.
uvicorn[standard]==0.24.0   ##  Uvicorn is the server that actually runs the FastAPI application.
python-multipart==0.0.6 ## Used when  API needs to handle form data and file uploads.
psycopg2-binary==2.9.9  ## This is a PostgreSQL database driver for Python. Allows Python to communicate with PostgreSQL
python-dotenv==1.0.0 ## helps to load the environment variable from the .env
pandas==2.1.3 ## Python library for - working with structured/tabular data 
openpyxl==3.1.2 ## OpenPyXL is used to work with Excel .xlsx files.
pytest==7.4.3 ## Pytest is used for testing the Python application.
Faker==20.1.0 ## o generate names, emails, and dates. They define a "factory" that generates entities with referential integrity 
bcrypt==5.0.0 ## Password hashing library for HR authentication
pyjwt==2.13.0 ## JSON Web Token encode/decode for HR authentication
httpx==0.25.2 ## HTTP client for test suite and API interactions


